# TODOS

## ~~고스트 시드 공유 설계 결정 (데일리 시드 여부)~~ → 결정됨 (2026-06-12)
- **결정: 데일리 시드 채택.** UTC 날짜 기반 시드(`src/dailySeed.ts`) — 같은 날 = 같은 코스.
  고스트 의미론은 오버레이(충돌 없음, 반투명 렌더). Phase 2 셀프 고스트 MVP에 구현됨.
- 근거: Phase 0 테스터의 패턴 학습 본능(OBSERVATIONS #1) + 코스 공유 없이는 고스트가
  "헛것을 피하는 유령"이 됨. 암기 메타는 데일리 리셋이 자연 완화 (날마다 새 코스).
- 잔여: 크로스유저 확장 시 "오늘의 코스" 리더보드로 그대로 사용 (백엔드 단계).

## 밸런스 패치 시 고스트 무효화 운영 정책
- **What:** 물리값/밸런스 패치 시 기존 고스트 로그 전체가 무효화되는 문제의 운영 정책: (a) 패치마다 리더보드/고스트 초기화, (b) 구버전 시뮬 번들 보관해 구버전 고스트 재생 지원, (c) 시즌제 운영(주기적 리셋을 기획으로 전환).
- **Why:** 디자인 문서는 "버전 불일치 고스트 제외"까지만 정했고, 유저 자산이 주기적으로 증발하는 게임을 어떻게 운영할지는 비어 있음. 조작감을 계속 다듬을 게임이라 패치는 필연 (외부 검토 #6).
- **Pros:** (c) 시즌제는 운영 함정을 기획 기회로 전환 가능 — 주간 리더보드 리셋 = 신규/복귀 유저도 1등 도전 가능.
- **Cons:** 지금 결정해도 2단계 전까지 실행할 게 없음. 방향만 정해두는 수준.
- **Context:** 버전 태그 설계는 이미 1단계 범위(시뮬 버전 태그). 이 TODO는 그 위의 운영/기획 레이어. 2단계 설계 시작 전에 방향 결정.
- **Depends on:** 2단계 설계 시작 전
- **2026-06-18 보강:** 메커니즘 변경(콤보/피버/물리/크기/패턴) = sim = SIM_VERSION 업 = 고스트·리더보드 무효화. **론칭 전(지금)엔 무료, 론칭 후엔 시즌으로 묶어야 함.** 연출 변경(색/문구/레이아웃)은 버전 무관 → 언제든 자유.

---

## 캐릭터 선택 + 캐릭터별 스탯 차등 (론칭 후, 데이터 기반)
- **What:** 게임 시작 시 달리는 캐릭터 선택. 캐릭터별 점프 횟수 / 체력 / 속도 차등.
- **Why:** 리텐션/깊이 기능. 핵심 루프엔 불필요(단일 캐릭터로 Go/No-Go 통과, 8연속 플레이).
- **When:** 론칭 후 실데이터로 밸런스 → 시즌 콘텐츠("새 캐릭터 출시")로. 지금 하면 백엔드가 밀리고 밸런스 데이터도 없음.
- **성격 분리:**
  - 외형만 선택 = 연출(버전 무관, 에셋 필요) → 에셋 단계 가능.
  - 스탯 차등 = 메커니즘(sim, 버전 업). 고스트 재생 위해 로그에 캐릭터 정보 필요.
- **주의 — 리더보드 공정성:** 속도 차등은 거리점수(=속도×시간) 직결이라 빠른 캐릭터가 항상 유리 → 캐릭터별 리더보드 분리 or 정규화 필요. (점프 횟수 차등은 점수에 직접 영향 적음)
- **고스트 렌더 결정(2026-06-18):** 고스트는 캐릭터 무관 **한 디자인으로 통일 렌더**(에셋 절감 + 시각 정리). 단 "똑같이 보이기 ≠ 똑같이 움직이기" — 로그엔 characterId를 담아 **궤적은 그 캐릭터 스탯대로 재생**해야 함(안 그러면 desync). 렌더만 무시.
- **Forward-design (지금 거의 공짜, 백엔드 로그 스키마 설계 시):** 입력 로그 스키마에 `characterId` 슬롯 예약(기본값 'base'), `parseLog`은 필드 없으면 'base'로 관대하게. → 나중에 캐릭터 추가가 *가법적*(기존 스탯 불변 시 버전 업 없이 추가). 안 해두면 추가 시점에 그때의 실유저 고스트 전부 무효화.
- **Depends on:** 론칭 + PostHog 실데이터. Forward-design만 백엔드 스키마 설계 때.

## 에셋 / 비주얼 폴리시 (Go 통과 후 자격 있음 — 에셋 단계)
- **What:** 캐릭터/장애물/배경 스프라이트, 캐릭터 선택 화면 UI, 전반적 비주얼.
- **포함 — 가로 화면 잔여 기둥(pillarbox) 마감:** WORLD_WIDTH 와이드화 후에도 기기 비율 차로 좌우에 남는 빈 공간을 **배경 아트로 덮기**(시차 배경 등). ★ 플레이 폭(WORLD_WIDTH)은 고정 유지 — 늘려서 채우면 크로스유저 고스트 깨짐.
- **When:** 백엔드/론칭 준비와 함께 또는 그 후. `/plan-design-review`로 방향 잡기.
- **Why 지금 아님:** 게이트 전 에셋 = 게임 뒤집힐 때 같이 버려짐. 검증·측정된 게임에 거는 증폭기.

## 실기기(iOS) 결정론 검증 (백엔드/크로스유저 전 필수)
- **What:** 진짜 아이폰(JSC 엔진)에서 골든 리플레이가 안 깨지는지 확인.
- **Why:** 크롬 DevTools는 항상 V8/Blink라 iOS JSC의 부동소수점 차이를 못 드러냄. D10(사칙연산 제약)이 의도대로 교차 엔진 결정론을 보장하는지는 실기기에서만 검증됨.
- **When:** 크로스유저 고스트(iOS+Android 혼재)를 신뢰하기 전. 셀프 고스트만이면 후순위.

## WebView / 플랫폼 통합 (슈퍼앱 연동 단계)
- 노치/세이프에어리어 처리(실기기 보고), 유저 식별(슈퍼앱 계정 연동 — T1: 출시 시), WebView 배포(플랫폼 자체 프로세스). 식별 전까지 Phase 1은 익명/로컬.
